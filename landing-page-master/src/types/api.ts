export type LogoProps = {
	alternativeText: string
	url: string
 }

 export type HeaderProps = { 
	 title: string
     description: string
 }

export type landingPageProps = {
	logo: LogoProps
    header: HeaderProps
}